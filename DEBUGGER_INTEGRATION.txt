┌─────────────────────────────────────────────────────────────────────────┐
│                     DEBUGGER AGENT INTEGRATION                          │
└─────────────────────────────────────────────────────────────────────────┘

FLOW DIAGRAM:
═════════════

┌──────────────────┐
│  Orchestrator    │
│    (main.py)     │
└────────┬─────────┘
         │
         ├──────────────────────────────────────────────────────────┐
         │                                                           │
         v                                                           v
┌─────────────────┐                                        ┌──────────────────┐
│ Optimal Agent   │─── Generates Code ──>                 │  Brute Agent     │
│ (attempt N)     │                                        │  (reference)     │
└────────┬────────┘                                        └──────┬───────────┘
         │                                                         │
         v                                                         v
┌─────────────────┐                                        ┌──────────────────┐
│   Executor      │                                        │   Executor       │
│ (run solution)  │                                        │ (run reference)  │
└────────┬────────┘                                        └──────┬───────────┘
         │                                                         │
         v                                                         v
┌─────────────────┐                                        ┌──────────────────┐
│ Actual Output   │                                        │ Expected Output  │
└────────┬────────┘                                        └──────┬───────────┘
         │                                                         │
         └──────────────────────┬──────────────────────────────────┘
                                │
                                v
                        ┌───────────────┐
                        │  Comparator   │
                        └───────┬───────┘
                                │
                    ┌───────────┴──────────┐
                    │                      │
                    v                      v
              ┌──────────┐          ┌──────────┐
              │  MATCH   │          │ MISMATCH │
              │ Success! │          │          │
              └──────────┘          └─────┬────┘
                                          │
                    ┌─────────────────────┘
                    │
                    v
        ┌───────────────────────────┐
        │    🔍 DEBUGGER AGENT      │ ◄─── NEW COMPONENT
        └───────────┬───────────────┘
                    │
        ┌───────────┴─────────────────────────┐
        │                                     │
        v                                     v
┌────────────────────┐            ┌──────────────────────┐
│ Add Debug Prints   │            │  Execute w/ Debug    │
│ (to stderr)        │            │  (capture stderr)    │
└──────┬─────────────┘            └──────┬───────────────┘
       │                                 │
       │                                 v
       │                        ┌────────────────────┐
       │                        │  Debug Trace       │
       │                        │  (stderr output)   │
       │                        └──────┬─────────────┘
       │                               │
       └───────────┬───────────────────┘
                   │
                   v
         ┌──────────────────────┐
         │  Analyze Debug Trace │
         │  (find root cause)   │
         └──────┬───────────────┘
                │
                v
         ┌──────────────────────┐
         │  Enhanced Feedback   │
         │  - Original Diff     │
         │  - Debug Analysis    │
         │  - Execution Trace   │
         │  - Fix Suggestions   │
         └──────┬───────────────┘
                │
                v
         ┌──────────────────────┐
         │  Next Attempt (N+1)  │
         │  with better context │
         └──────────────────────┘


FILES CREATED/MODIFIED:
═══════════════════════

NEW FILES:
✓ agents/debugger_agent.py          - Core debugger agent implementation
✓ test_debugger.py                  - Standalone test demonstrating functionality
✓ DEBUGGER_FEATURE.md               - Complete documentation

MODIFIED FILES:
✓ agents/__init__.py                - Added DebuggerAgent import
✓ utils/executor.py                 - Added execute_with_debug() method
✓ orchestrator.py                   - Integrated debugger into main loop
✓ config.yaml                       - Added debugger_agent model config


KEY FEATURES:
═════════════

1. Non-Invasive Debug Instrumentation
   - Only adds print() to stderr
   - Preserves original stdout output
   - Doesn't modify algorithm logic

2. Strategic Debug Placement
   - Input parsing verification
   - Intermediate calculation steps
   - Loop iteration tracking
   - Decision branch logging
   - Final value inspection

3. Intelligent Analysis
   - Examines debug trace
   - Compares with expected behavior
   - Identifies divergence points
   - Suggests specific fixes

4. Enhanced Feedback Loop
   - Combines diff + trace + analysis
   - Provides actionable insights
   - Helps agent learn from mistakes
   - Reduces attempts needed


USAGE:
══════

The debugger automatically activates when:
- Optimal solution executes successfully
- But output doesn't match brute force

No manual intervention needed - it's fully integrated into the orchestrator!


EXAMPLE OUTPUT:
═══════════════

When a solution fails:
  ✗ Outputs don't match
  Difference: Expected: 15, Got: 10...
  🔍 Running debug analysis...
  📋 Debug output captured (456 chars)
  💡 Debug Analysis: The loop uses range(n) which gives 0 to n-1, 
     but should use range(1, n+1) to sum from 1 to n inclusive.
  
This detailed feedback goes to the next attempt!
